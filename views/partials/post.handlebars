<a href="/viewpost/{{id}}">
<div class="card">
    <h3 class="post-header">{{title}}</h3>
    <h4>Post by {{user.username}} on {{formatData data}}</h4>
    <div class="container">
        <p class="past-text">{{body}}</p>
    
    </div>
</div>
</a>
